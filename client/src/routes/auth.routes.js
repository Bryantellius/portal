import DefaultLayout from '../features/layout/DefaultLayout';

const authRoutes = [
  {
    path: '/login',
    exact: true,
    component: Login,,
    layout: EmptyLayout
  }
];

export default authRoutes;